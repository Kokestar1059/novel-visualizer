# 世界A（解析パイプライン）の依存。ローカル実行のみ（Sakuraには入れない・ADR-001）。
# GiNZA = spaCy 上で動く日本語解析（形態素・固有表現・係り受け）。決定論的（ADR-002）。
#
# セットアップ（手元PC・仮想環境推奨）:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r pipeline/requirements.txt
#   python pipeline/analyze.py
#
# ja_ginza は日本語モデル本体。ginza が spaCy 等の依存を引き込む。
ginza>=5.2.0,<5.3.0
ja_ginza>=5.2.0,<5.3.0
